<h1 id="prelude-ls">prelude.ls <a href="https://travis-ci.org/gkz/prelude-ls"><img alt="Build Status" src="https://travis-ci.org/gkz/prelude-ls.png?branch=master" /></a></h1>

<p>is a functionally oriented utility library. It is powerful and flexible. Almost all of its functions are curried. It is written in, and is the recommended base library for, <a href="http://livescript.net">LiveScript</a>.</p>

<p>See <strong><a href="http://preludels.com">the prelude.ls site</a></strong> for examples, a reference, and more.</p>

<p>You can install via npm <code class="prettyprint">npm install prelude-ls</code></p>

<h3 id="development">Development</h3>

<p><code class="prettyprint">make test</code> to test</p>

<p><code class="prettyprint">make build</code> to build <code class="prettyprint">lib</code> from <code class="prettyprint">src</code></p>

<p><code class="prettyprint">make build-browser</code> to build browser versions</p>
