<h1 id="lodash-v4-15-0">lodash v4.15.0</h1>

<p>The <a href="https://lodash.com/">Lodash</a> library exported as <a href="https://nodejs.org/">Node.js</a> modules.</p>

<h2 id="installation">Installation</h2>

<p>Using npm:
<code class="prettyprint">bash
$ {sudo -H} npm i -g npm
$ npm i --save lodash
</code></p>

<p>In Node.js:
&ldquo;`js
// Load the full build.
var _ = require(&lsquo;lodash&rsquo;);
// Load the core build.
var _ = require(&#39;lodash/core&rsquo;);
// Load the fp build for immutable auto-curried iteratee-first data-last methods.
var fp = require(&#39;lodash/fp&rsquo;);</p>

<p>// Load a method category.
var array = require(&#39;lodash/array&rsquo;);
var object = require(&#39;lodash/fp/object&rsquo;);</p>

<p>// Load a single method for smaller builds with browserify/rollup/webpack.
var chunk = require(&#39;lodash/chunk&rsquo;);
var extend = require(&#39;lodash/fp/extend&rsquo;);
&rdquo;`</p>

<p>See the <a href="https://github.com/lodash/lodash/tree/4.15.0-npm">package source</a> for more details.</p>

<p><strong>Note:</strong><br>
Don’t assign values to the <a href="http://nodejs.org/api/repl.html#repl_repl_features">special variable</a> <code class="prettyprint">_</code> in the Node.js &lt; 6 REPL.<br>
Install <a href="https://www.npmjs.com/package/n_">n_</a> for a REPL that includes <code class="prettyprint">lodash</code> by default.</p>

<h2 id="support">Support</h2>

<p>Tested in Chrome 51-52, Firefox 47-48, IE 9-11, Edge 14, Safari 8-9, Node.js 0.10-6, &amp; PhantomJS 2.1.1.<br>
Automated <a href="https://saucelabs.com/u/lodash">browser</a> &amp; <a href="https://travis-ci.org/lodash/lodash/">CI</a> test runs are available.</p>
