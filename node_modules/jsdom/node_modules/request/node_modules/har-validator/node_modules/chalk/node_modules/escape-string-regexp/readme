<h1 id="escape-string-regexp">escape-string-regexp <a href="https://travis-ci.org/sindresorhus/escape-string-regexp"><img alt="Build Status" src="https://travis-ci.org/sindresorhus/escape-string-regexp.svg?branch=master" /></a></h1>

<blockquote>
<p>Escape RegExp special characters</p>
</blockquote>

<h2 id="install">Install</h2>
<pre class="highlight plaintext"><code>$ npm install --save escape-string-regexp
</code></pre>

<h2 id="usage">Usage</h2>
<pre class="highlight javascript"><code><span class="kr">const</span> <span class="nx">escapeStringRegexp</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="s1">'escape-string-regexp'</span><span class="p">);</span>

<span class="kr">const</span> <span class="nx">escapedString</span> <span class="o">=</span> <span class="nx">escapeStringRegexp</span><span class="p">(</span><span class="s1">'how much $ for a unicorn?'</span><span class="p">);</span>
<span class="c1">//=&gt; 'how much \$ for a unicorn\?'</span>

<span class="k">new</span> <span class="nb">RegExp</span><span class="p">(</span><span class="nx">escapedString</span><span class="p">);</span>
</code></pre>

<h2 id="license">License</h2>

<p>MIT © <a href="http://sindresorhus.com">Sindre Sorhus</a></p>
