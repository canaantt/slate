<h1 id="xtend">xtend</h1>

<p><a href="http://ci.testling.com/Raynos/xtend"><img alt="browser support" src="http://ci.testling.com/Raynos/xtend.png" /></a></p>

<p><a href="http://github.com/badges/stability-badges"><img alt="locked" src="http://badges.github.io/stability-badges/dist/locked.svg" /></a></p>

<p>Extend like a boss</p>

<p>xtend is a basic utility library which allows you to extend an object by appending all of the properties from each object in a list. When there are identical properties, the right-most property takes precedence.</p>

<h2 id="examples">Examples</h2>
<pre class="highlight javascript"><code><span class="kd">var</span> <span class="nx">extend</span> <span class="o">=</span> <span class="nx">require</span><span class="p">(</span><span class="s2">"xtend"</span><span class="p">)</span>

<span class="c1">// extend returns a new object. Does not mutate arguments</span>
<span class="kd">var</span> <span class="nx">combination</span> <span class="o">=</span> <span class="nx">extend</span><span class="p">({</span>
    <span class="na">a</span><span class="p">:</span> <span class="s2">"a"</span><span class="p">,</span>
    <span class="na">b</span><span class="p">:</span> <span class="s1">'c'</span>
<span class="p">},</span> <span class="p">{</span>
    <span class="na">b</span><span class="p">:</span> <span class="s2">"b"</span>
<span class="p">})</span>
<span class="c1">// { a: "a", b: "b" }</span>
</code></pre>

<h2 id="stability-status-locked">Stability status: Locked</h2>

<h2 id="mit-licenced">MIT Licenced</h2>
