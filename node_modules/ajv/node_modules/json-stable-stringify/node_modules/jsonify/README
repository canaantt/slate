<h1 id="jsonify">jsonify</h1>

<p>This module provides Douglas Crockford&rsquo;s JSON implementation without modifying
any globals.</p>

<p><code class="prettyprint">stringify</code> and <code class="prettyprint">parse</code> are merely exported without respect to whether or not a
global <code class="prettyprint">JSON</code> object exists.</p>

<h1 id="methods">methods</h1>

<p>var json = require(&lsquo;jsonify&rsquo;);</p>

<h2 id="json-parse-source-reviver">json.parse(source, reviver)</h2>

<p>Return a new javascript object from a parse of the <code class="prettyprint">source</code> string.</p>

<p>If a <code class="prettyprint">reviver</code> function is specified, walk the structure passing each name/value
pair to <code class="prettyprint">reviver.call(parent, key, value)</code> to transform the <code class="prettyprint">value</code> before
parsing it.</p>

<h2 id="json-stringify-value-replacer-space">json.stringify(value, replacer, space)</h2>

<p>Return a string representation for <code class="prettyprint">value</code>.</p>

<p>If <code class="prettyprint">replacer</code> is specified, walk the structure passing each name/value pair to
<code class="prettyprint">replacer.call(parent, key, value)</code> to transform the <code class="prettyprint">value</code> before stringifying
it.</p>

<p>If <code class="prettyprint">space</code> is a number, indent the result by that many spaces.
If <code class="prettyprint">space</code> is a string, use <code class="prettyprint">space</code> as the indentation.</p>
